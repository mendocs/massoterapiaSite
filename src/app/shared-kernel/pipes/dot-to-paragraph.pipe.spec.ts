import { DotToParagraphPipe } from './dot-to-paragraph.pipe';

describe('DotToParagraphPipe', () => {
  it('create an instance', () => {
    const pipe = new DotToParagraphPipe();
    expect(pipe).toBeTruthy();
  });
});
