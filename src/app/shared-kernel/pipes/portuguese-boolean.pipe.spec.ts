import { PortugueseBooleanPipe } from './portuguese-boolean.pipe';

describe('PortugueseBooleanPipe', () => {
  it('create an instance', () => {
    const pipe = new PortugueseBooleanPipe();
    expect(pipe).toBeTruthy();
  });
});
